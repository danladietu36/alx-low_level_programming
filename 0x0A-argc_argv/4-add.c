#include <stdio.h>
#include "main.h"

/**
 * main - adds positive numbers
 * @argc: Argument counts
 * @argv: Argument vector
 *
 * Return: Always 0.
 */
int main(int argc, char argv[])
{
	int i, n, sum = 0;

	for (i = 0; i < argc; i++)
	{
		if (i 

