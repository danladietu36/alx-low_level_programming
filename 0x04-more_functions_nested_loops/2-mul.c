#include "main.h"

/**
 * mul - multiply two integers
 * @a: First of the parameters to multiply
 * @b: Second parameter to multiply
 * Return: Always 0
 */
int mul(int a, int b)
{
	int prod;

	prod = a * b;

	return (prod);

}
