#include "main.h"

/**
 * more_numbers - prints 0 to 14 ten times
 * Return: Always 0
 */
void more_numbers(void)
{
	int num, a;

	a = 0;

	for (a = 0; a <= 9; a++)
	{
		num = 0;
		for (num = 0; num = 14; num++)
		{
			_putchar(a + '0');
		}
	_putchar('\n');
	}
}
