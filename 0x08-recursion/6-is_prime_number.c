#include "main.h"

/**
 * is_prime_number- get prime integer of a number
 * @n: integer input
 *
 * Return: Always 0
 */
int is_prime_number(int n)
{
	int num;

	if (num % n != 0)
		return (1);
	else
		return (0);
}
